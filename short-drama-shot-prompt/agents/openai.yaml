interface:
  display_name: "Short Drama Shot Prompt"
  short_description: "Write one image prompt and one short caption for one approved shot."
  default_prompt: "Use $short-drama-shot-prompt to turn this one approved shot JSON into one clean still-image prompt plus one short caption line, while trusting anchors and writing from image space, not story-summary space."
